name: first-workflow

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Hello World
              run: echo "Hello World"

            - name: list files
              run: ls
              
            - name: read files
              run: cat README.md  